// Copy to secrets.h (same folder) and fill in. secrets.h is git-ignored.
#pragma once
#define WIFI_SSID    "CHANGE_ME_SSID"               // 2.4 GHz network - ESP32 cannot see 5 GHz
#define WIFI_PASS    "CHANGE_ME_PASSWORD"
#define GATEWAY_URL  "http://CHANGE_ME_HOST:8000"   // laptop LAN IP, no trailing slash
