.venv/
__pycache__/
*.pyc
.env

# local tooling / editor state
.claude/
.opencode/

# training noise (models + progress.csv are committed)
rl/train_log.txt
.opencode
.impeccable/

# generated / transient doc artifacts
docs/*.backup.pptx
docs/*.backup.docx
docs/~$*

# editor state + internal working notes
.vscode/
FINALS_PROMPT.md
__pycache__/
.pytest_cache/

# generated historical dataset (rebuild: python -m scripts.generate_dataset)
data/dataset/*.sqlite
data/dataset/*.building

# security material: users (password hashes), device keys, audit logs, TLS keys/certs
data/security/
*.pem
*.key
*.crt
*.p12

# firmware credentials (template: secrets.h.example)
hardware/firmware/feelslike_node/secrets.h

# review screenshots from assistant sessions (not product)
Claude outputs/
